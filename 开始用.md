# 已安装但打不开：修复后继续用

解压最新下载包，双击 **修复并打开.command**。原来的会议和设置会保留，不要删除资料目录。修复完成会打开听会台。

正在录音时不会自动重启旧服务；结束会议后再双击「启动.command」。若系统提示阻止打开，在 macOS「系统设置 → 隐私与安全性」核对来源后允许打开。

模型尚未配置也可使用本机转写；实时翻译、分析和智能纪要需要可用的模型服务。Python未安装不阻止打开和录音，但会后自动整理要安装Python后重试。公开包不包含任何人的API凭据。

---

# 听会台 · 开箱就能用

不用注册、不用充值。转写用的是 Mac 自带的语音识别，装完就能开会。
会后想要整理好的纪要，需要一个模型的 key——Aaron 会微信发给你，多一步粘贴，也可以先不管。
按下面四步走，第一次大概五分钟，以后每次开会点一下就行。

---

## 第一步：把这个文件夹放好

把解压出来的整个文件夹拖到「文稿」或者桌面，放一个以后不会误删的地方。
（放好再往下做，装完之后不要再移动它。）

---

## 第二步：先解开 macOS 的下载保护（只做一次）

从网上下载的东西，macOS 默认不让直接运行，双击会说「打不开，要移到废纸篓」。**这不是文件有问题**，按下面四步解开就行，一分钟。

1. 按 **⌘ + 空格**，输入 `终端`，回车，打开终端窗口。
2. 在终端里输入这一串（**末尾留一个空格，先别回车**）：

   ```
   xattr -dr com.apple.quarantine 
   ```

3. 把第一步放好的那个**文件夹**，直接用鼠标拖进终端窗口。拖进去后它会自动补上一段路径。
4. 按回车。没有任何提示就是成功了。

做完这一步，终端可以关掉了。

---

## 第三步：双击「安装.command」

窗口里会自己滚一些字，看到最后写着安装完成就好了。
第一次会下载运行环境，用时几分钟，中途不要关窗口，也不需要输密码。

（如果还是说「无法验证开发者」，在文件上点右键 → 打开 → 再点一次「打开」。）

---

## 第四步：双击「启动.command」

浏览器会自己打开听会台的页面。**页面出来就可以开会了**：点右上角红色的「开始听会」，系统弹出「语音识别」和「麦克风」的授权时点允许。

第一次可能先打开一个「首次设置」的页面。那一页什么都不用填：确认「转写方式」选的是「本机转写」，直接点「保存」，再点「进入听会台」。

以后每次开会，只要双击「启动.command」这一个文件。

---

## 想要会后纪要（可选，随时都能做）

会中的字幕、要点不需要任何 key。开完会想让它写一份整理好的纪要，需要一个模型的 key。
Aaron 会在微信上发给你一串以 `sk-` 开头的字。收到后：

1. 打开听会台页面，点右下角「设置」→ 点「填 API Key」，会打开一个页面。
2. 找到「模型 API Key」那一栏，把那串字粘进去，点「保存」。

没有这一步也能正常开会，只是会后不会自动出纪要。

---

## 开会怎么用

1. 页面上先选**这场会怎么开的**：
   - 线上会（腾讯会议 / Zoom / Teams / Meet）→ 按提示让浏览器共享那个标签页的声音
   - 面对面开会 → 选线下，把电脑放桌上就行
2. 点 **开始听会**。
3. 左边是实时字幕，中间是它自己整理的要点，右边是它觉得需要你核实一下的地方。
4. 开完点 **结束并总结**，等一会儿就有整理好的纪要。

想看以前的会：点底部 **历史会议**，一场一张卡，点「打开」就能看全文和总结。

---

## 几个小提醒

- **会议内容只存在你自己的电脑上**，不会传给我，也不会传到公司。
- 说什么语言它自己会认，中文英文混着说也没问题。印尼语、葡语、西语也能转，只是会中的字幕会糙一点，开完会它会自己重新转一遍准的。
- 开会时**别让电脑睡着**，合盖或者息屏会断掉。
- 手机上不能录，得用电脑。

---

## 出问题了怎么办

| 遇到 | 怎么办 |
|---|---|
| 打开后还让我填「火山语音应用凭证」 | 说明你之前装过旧版本。把这个新文件夹里的「启动.command」再双击一次，它会自动把配置补上；要是还问，就把旧文件夹删掉再走一遍第二步 |
| 双击说「打不开，要移到废纸篓」 | 第二步没做或没做成，回去重做一遍第二步 |
| 双击没反应 / 说无法验证开发者 | 在文件上点右键 → 打开 → 再点一次「打开」 |
| 页面打不开 | 先双击一次「安装.command」，再双击「启动.command」 |
| 有字幕但没有要点 | 等 40 秒左右，它是每隔一会儿整理一次 |
| 完全不出字 | 双击「自检.command」，把窗口里的字截图发给 Aaron |
| 其他任何问题 | 截图发给 Aaron |

---

## 关于里面的钥匙

这个文件夹里有一个 `preset.json`，里面是 Aaron 的语音和 AI 账号凭据，专门配给你用的。
**这个文件夹不要转发给别人**，别人要用的话让他找 Aaron 要。
