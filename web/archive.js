'use strict';
let record;
fetch('/asr-relay/meeting-result?id='+encodeURIComponent(new URLSearchParams(location.search).get('id')||''),{cache:'no-store'}).then(async r=>{if(!r.ok)throw Error('尚未完成整理，请回到会议档案查看状态。');return r.json();}).then(s=>{record=s;document.getElementById('title').textContent=s.title||'未命名会议';document.getElementById('summary').textContent=s.summary||'总结尚未完成，逐字稿已保留。';for(const t of s.transcript||[]){const p=document.createElement('p');p.textContent='['+(t.t||t.at||'')+'] '+t.text;document.getElementById('transcript').append(p);if(t.originalText&&t.originalText!==t.text){const x=document.createElement('small');x.textContent='原句：'+t.originalText;document.getElementById('transcript').append(x);}}}).catch(e=>document.getElementById('title').textContent=e.message);
document.getElementById('download').onclick=()=>{if(!record)return;const text='# '+(record.title||'会议')+'\n\n'+(record.summary||'')+'\n\n'+record.transcript.map(t=>'['+(t.t||t.at||'')+'] '+t.text+(t.originalText&&t.originalText!==t.text?'\n原句：'+t.originalText:'')).join('\n\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([text],{type:'text/markdown;charset=utf-8'}));a.download='会议记录.md';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);};
