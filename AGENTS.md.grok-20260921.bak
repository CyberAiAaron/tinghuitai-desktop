# Cline 强制规则（Aaron · Grok 审核门禁）

你是施工助手。Grok Bot 负责审核。两边不自动互通，靠落盘。

## 门禁（必须遵守）
1. **先 Plan**：只把计划写入 `~/Workbuddy/agent_bus/cline_plan_pending.md`，把 `~/Workbuddy/agent_bus/cline_review_status.md` 写成 `waiting_plan`，然后**停止**。不得改 `app/` `web/` `scripts/` 等业务代码。
2. **等审核**：只有状态变成 `plan_approved` 才能动手。
3. **做完再停**：写入 `~/Workbuddy/agent_bus/cline_result_pending.md`，状态 `waiting_result`，停止。等 Grok 验收 `result_approved` 才算过关；未通过则按意见改，再写结果文件。
4. 详读：`~/This is my Chansey/Meeting LiveMate/Cline与Grok审核协议_20260921.md`
5. 听会台本轮只做 Slice A（THT-R1+R2+R4）；工程真源是本仓库（Desktop 下 tinghuitai-desktop），不要用已删的 Workbuddy 拷贝。
6. 禁止听会台内「我在发言」按钮（R11）。
7. 动手前看 `~/Workbuddy/agent_bus/claims.md`；`activeSessions>0` 不重启服务。

若你正在狂跑工具：立刻停止，把「已改了什么」写入 result 文件，状态 `waiting_result`，等审核。

